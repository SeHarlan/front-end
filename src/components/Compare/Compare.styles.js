import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles((theme) => ({
  root: {
    paddingTop: '3rem',
    paddingBottom: '5rem',
  },
  pageHeader: {
    marginBottom: '2rem'
  },
  formControl: {
    marginBottom: '3rem',
    marginRight: '1rem'
  },
  chipMarginTop: {
    marginBottom: '2rem',
    marginRight: '10px',
  },
  chipMarginBottom: {
    marginTop: '2rem',
    marginRight: '10px',
  },


}));
